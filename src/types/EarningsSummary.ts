export interface EarningsSummary {
  totalEarnings: number; // Total earnings for the period
  earnedThisWeek: number; // Earnings for the current week
  earnedThisMonth: number; // Earnings for the current month
  earnedThisYear: number; // Earnings for the current year
  averageJobValue: number; // Average value of jobs completed
  highestJobValue: number; // Highest value job completed
}
